# runtime / volumes
data/
backups/
runtime/state.json
runtime/jobs/

# workspace (output)
workspace/

# python junk
__pycache__/
*.pyc
*.log
*.tmp
*.swp
.DS_Store

# workspace (output)
workspace/

workspace/
runtime/agent-router-root/


# runtime state (generated)
runtime/backlog.json
runtime/state.json
runtime/jobs/

# ui junk
ui/node_modules/
ui/*.bak*
ui/**/*.bak*

# runtime autopilot
runtime/autopilot/
.secrets/
.env.toconline
.toc_token.json
.toc_token_expiry

# TLS — chaves privadas nunca vão para git
tls/*.pem
tls/*.key

# Caddy binary e runtime
bin/caddy
runtime/secrets.json
runtime/pending_approvals.json
runtime/caddy_access.log

# secrets / tokens (NEVER COMMIT)
.env*
.toc_token.json
.toc_token_expiry
runtime/*

# local/runtime noise
tmp/
runtime/
*.bak.*
bin/*.bak.*
dashboard/
dashboards/
backlog/
*_dashboard.html
bin/autofill.py
