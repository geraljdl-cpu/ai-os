# Auth dependencies — instaladas no .venv em nodecpu
# IMPORTANTE: bcrypt deve ser < 5.0.0 (passlib 1.7.4 incompatível com bcrypt>=5.0)
# Ver: https://github.com/pypa/passlib/issues/720
passlib==1.7.4
bcrypt==4.0.1
python-jose==3.5.0
