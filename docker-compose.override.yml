services:
  agent-router:
    env_file:
      - .env
    ports:
      - "5679:5679"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./agent-router/patch:/app
      - ./runtime:/app/runtime
      - /home/jdl/ai-os:/host/ai-os
